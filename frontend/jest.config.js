// Using CRA's default Jest configuration with jsdom environment
module.exports = {
  // Define the test environment to use jsdom for React components
  testEnvironment: 'jest-environment-jsdom',
  
  // Let Create React App handle the rest of the configuration
  // This ensures compatibility with CRA's defaults
};
